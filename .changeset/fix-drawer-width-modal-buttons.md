---
'@repo/ui': major
---

Ensure side drawers maintain a minimum width of 20rem on small screens and allow full width on larger viewports. Update modal buttons so the confirm button uses a solid primary style and appears before the cancel button.
